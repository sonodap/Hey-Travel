<div class='container'>
  <div class='row'>
    <div class='col-md-12'>
      <h2 class='my-md-4'>会員の登録情報</h2>
      <table class='table'>
        <thead>
          <tr>
            <td>会員ID</td>
            <td>名前</td>
            <td>ステータス</td>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <th><%= user.id %></td>
              <td><%= link_to user.name, admin_user_path(user.id) %></td>
              <td>
                <% if user.is_deleted == false %>
                  <p>有効</p>
                <% elsif %>
                  <p class="text-danger">退会</p>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      
    </div>
  </div>
</div>